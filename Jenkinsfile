

pipeline {
// Example usage
    agent any

    stages {
        stage('Example') {
            steps {

                echo " ${env.GIT_TAG_NAME} ${env.GIT_TAG_MESSAGE}"

            }
        }
    }
}
