pipeline {
agent any

 stages {

        stage('VAR') {

            steps { 
             git branch: 'main', url: 'https://github.com/zhvakinde/jenkins' 
             script {
             env.Commit = sh(script: 'git rev-parse HEAD', returnStdout: true)?.trim()
             env.TAG_NAME= sh "git describe --tags ${env.Commit} --abbrev=0"
}
           echo env.TAG_NAME
}
}
}

}
